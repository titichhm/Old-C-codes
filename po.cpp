#include<bits/stdc++.h>
using namespace std;
int main()
{
	int t;
	cin>>t;
	while(t!=0)
	{
		t--;
		int n,temp;
		cin>>n;
		vector<int> s;
		for(int i=0;i<n;i++)
		{
			cin>>temp;
			s.push_back(temp);
		}
		//loop can run until s[0]!=0
		while(s[0]!=0)
		{
			
		}
	}
	return 0;
}
